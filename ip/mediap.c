#include <stdio.h>
#include <stdlib.h>

int main(){

int a,b,c,d;
float mp;

scanf("%d", &a);
scanf("%d", &b);
scanf("%d", &c);
scanf("%d", &d);

mp= (a*1 + b*2 + c*3 + d*4)/10;

	printf ("media ponderada= %.2f", mp);

return 0;
}
